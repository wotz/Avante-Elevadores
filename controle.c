
/*
void fcfs(Elevador* e, Lista* l){
	//Lista all para operar
	Lista* all = createList();
	all = copyList(l);
	
	//Ponteiro auxiliar para percorrer all
	Node* node = createNode();
	node = l->begin;
	int i = 1;
	while(!isEmpety(all)){
		printNode(node);
		node = node->next;
		if(isUp(e->posicao, node->demand.origem))
	}
}*/
